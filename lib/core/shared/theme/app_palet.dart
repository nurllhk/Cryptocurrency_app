import 'package:flutter/material.dart';

@immutable
class Palet {
  // Todo Page Radius
  static const Radius pageRadius = Radius.circular(20);
  static final BorderRadius singleRadius = BorderRadius.circular(20);

  // Todo Page Padding

  static const EdgeInsets pagePadding =
      EdgeInsets.symmetric(vertical: 10, horizontal: 10);
}
